# SPDX-FileCopyrightText: Copyright © 2026 OpenCHAMI a Series of LF Projects, LLC
#
# SPDX-License-Identifier: MIT

type: remap
inputs:
  - logs_exclusiverouter.opaal
source: |-
  if .msg != "" {
    result, error = parse_json(string!(.msg))
    if error == null {
      .data = result
    } else {
      .parse_error = error
    }
  }
